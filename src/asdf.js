import * from 'Javascript';
import * from 'React';
import * from 'Nodejs';

let take_rest = watch(sky).then(wash(mind)).catch(err=>err.fix());
let survive = buyMask();

while(living) {
    find(purpose_of_life);
}












